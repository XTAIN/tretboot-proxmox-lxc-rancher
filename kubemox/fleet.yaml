defaultNamespace: kubemox

helm:
  chart: kubemox
  repo: https://alperencelik.github.io/helm-charts
  version: 0.2.1

  values:
    proxmox:
      insecureSkipTLSVerify: true
  valuesFrom:
    - configMapKeyRef:
        name: kubemox-values
        namespace: tretboot
        key: yaml